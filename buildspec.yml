version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"
  
  build:
    commands:
      - echo "Build started"
      - echo "Nothing to compile for static HTML"

  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
